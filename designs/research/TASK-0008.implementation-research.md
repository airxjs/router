# TASK-0008 Implementation Research Brief

## Task
对齐 peerDeps 到 airx@^0.4.0 并发布 0.3.0 稳定版

## Summary
将 airx-router 从 0.3.0-alpha.1 稳定发布为 0.3.0，对齐 peerDependencies 支持 airx@^0.4.0 和 ^0.6.0-beta。

---

## Design Guidelines and Specs

- `.projitive/roadmap.md` - ROADMAP-0002 milestone
- `package.json` - 发布配置和依赖声明
- `.github/workflows/check.yml` - CI 检查流程
- `.github/workflows/publish.yml` - npm 发布流程

---

## Code Changes Summary

### package.json

**Version upgrade:**
```diff
- "version": "0.3.0-alpha.1",
+ "version": "0.3.0",
```

**peerDependencies update:**
```diff
- "airx": "^0.3.0-alpha.1"
+ "airx": "^0.4.0 || ^0.6.0-beta"
```

---

## peerDeps Alignment Analysis

| airx-router | airx | Status |
|-------------|------|--------|
| 0.3.0       | ^0.4.0 | ✅ 稳定版支持 |
| 0.3.0       | ^0.6.0-beta | ✅ Beta 支持 |
| 0.3.0-alpha.1 | ^0.3.0-alpha.1 | 旧版本（已废弃） |

**对齐理由：** airx@0.4.0 是当前稳定主流版本，^0.6.0-beta 确保与下一代 airx 兼容。

---

## Test Verification

```
✓ source/router.test.ts (28 tests) 21ms
  Test Files  1 passed (1)
      Tests  28 passed (28)
  Duration    669ms
```

**所有 28 个测试全部通过，无回归问题。**

---

## Release Plan

1. **提交到 dev 分支** → 代码审核和 CI 验证
2. **合并到 main 分支** → 触发 CI build
3. **创建 GitHub Release** → 触发 `publish.yml` 自动发布到 npm
4. **更新任务状态** → TASK-0008 → DONE

### CI Pipeline
- `check.yml`: lint + build（push to main / PR）
- `publish.yml`: build + npm publish（release created）

### npm Registry
- Access: public（`npm publish --access public`）
- Package: `airx-router`
- 版本: `0.3.0`

---

## Risks & Notes

- **CI 在 dev 分支不运行**，需合并到 main 后才能触发完整检查
- **npm publish 依赖 GitHub Release 事件**触发 `publish.yml`
- 发布后需确认 npm registry 上的版本号和时间戳
